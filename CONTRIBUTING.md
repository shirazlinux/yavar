# مشارکت در یاور

از علاقه‌مندی‌تان به نرم‌افزار آزاد سپاسگزاریم.

## قبل از PR

1. از شاخهٔ به‌روز `main` شروع کنید.  
2. `config.php` و دیتابیس واقعی را commit نکنید.  
3. برای باگ امنیتی → گزارش خصوصی، نه Issue عمومی با PoC مخرب.

## سبک کد

- `declare(strict_types=1);`
- SQL فقط با prepared statement
- خروجی HTML با `e()`
- APIهای state-changing: CSRF + در صورت نیاز rate limit
- پیام‌های UI به فارسی روان

## تست دستی پیشنهادی

- ثبت‌نام / ورود / فراموشی رمز  
- شروع پرداخت بدون CSRF → باید 403  
- یک مسیر بانک + یک مسیر درگاه (sandbox)  

## مجوز مشارکت

با ارسال patch، مشارکت را تحت **AGPL-3.0-or-later** می‌پذیرید (همان مجوز پروژه).
